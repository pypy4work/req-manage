# Frontend environment variables

# Set to backend URL to enable real API calls (e.g., http://localhost:4000)
# If not set, the app will use the frontend mock (api.ts)
VITE_BACKEND_URL=

# Optional: front-end hints for unified webhook (used in mock mode only)
VITE_N8N_WEBHOOK_URL=
VITE_APPEALS_WEBHOOK_URL=
