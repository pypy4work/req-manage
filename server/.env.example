DB_HOST=localhost
DB_NAME=master
DB_USER=sa
DB_PASS=Your_password123
PORT=4000

# Dialect: mssql (افتراضي) أو postgres
DB_DIALECT=mssql

# في حالة Postgres يمكنك إما استخدام POSTGRES_URL
# POSTGRES_URL=postgres://sca_user:sca_pass@localhost:5432/sca_requests
# أو استخدام DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASS كما في الأعلى مع DB_DIALECT=postgres
DB_PORT=5432
DB_SSL=false

# Travel time API (car routing) - backend فقط
# مثال لمزود مجاني: OSRM self-hosted أو OpenRouteService (له خطة مجانية)
TRAVEL_API_PROVIDER=OSRM
TRAVEL_API_URL=https://router.project-osrm.org/route/v1/driving
TRAVEL_API_KEY=
