#!/usr/bin/env pwsh
# Transfer System Test Script
# اختبار نظام التنقلات

$BASE_URL = "http://localhost:3001"
$DELAY = 1000

Write-Host "🧪 بدء اختبار نظام التنقلات..." -ForegroundColor Cyan
Write-Host "================================" -ForegroundColor Cyan

# Test 1: Check if app is running
Write-Host "`n1️⃣ التحقق من تشغيل التطبيق..." -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "$BASE_URL/" -ErrorAction SilentlyContinue
    Write-Host "✅ التطبيق يعمل على $BASE_URL" -ForegroundColor Green
} catch {
    Write-Host "❌ لا يمكن الوصول للتطبيق. تأكد من تشغيل npm run dev" -ForegroundColor Red
    exit
}

# Test 2: Login as Employee
Write-Host "`n2️⃣ محاكاة تسجيل دخول الموظف..." -ForegroundColor Yellow
Write-Host "-> في المتصفح انقر على: Transfer Request" -ForegroundColor Magenta

# Test 3: Check if Transfer Form loads
Write-Host "`n3️⃣ التحقق من تحميل نموذج التنقل..." -ForegroundColor Yellow
Write-Host "-> تحقق من:
  ✓ نموذج تقديم الطلب يظهر
  ✓ يمكنك إضافة الوحدات المفضلة
  ✓ يمكنك ترتيب الأولويات
  ✓ زر التقديم يعمل" -ForegroundColor Cyan

# Test 4: Load as Manager
Write-Host "`n4️⃣ محاكاة تسجيل دخول المدير..." -ForegroundColor Yellow
Write-Host "-> سجل دخول باسم مختلف والتحقق من:
  ✓ عرض قائمة الطلبات المعلقة
  ✓ يمكنك النقر على طلب
  ✓ تظهر لوحة التقييم
  ✓ يمكنك إضافة تقييم" -ForegroundColor Cyan

# Test 5: Load as Admin
Write-Host "`n5️⃣ محاكاة تسجيل دخول الإدارة..." -ForegroundColor Yellow
Write-Host "-> تحقق من:
  ✓ تظهر لوحة الإحصائيات
  ✓ الرسم البياني يعمل
  ✓ زر تشغيل الخوارزمية موجود
  ✓ النتائج تظهر بنجاح" -ForegroundColor Cyan

Write-Host "`n✨ اختبارات يدوية مطلوبة من المتصفح" -ForegroundColor Green
Write-Host "
═══════════════════════════════════════════════
📋 قائمة الاختبار:
═══════════════════════════════════════════════

[ ] 1. نموذج تقديم الطلب:
    [ ] ملء سبب التنقل
    [ ] إضافة وحدات مفضلة
    [ ] ترتيب الأولويات
    [ ] التقديم بنجاح

[ ] 2. عرض طلباتي:
    [ ] ظهور الطلبات المرسلة
    [ ] عرض الحالة
    [ ] عرض التفاصيل

[ ] 3. مراجعة الطلبات (مدير):
    [ ] عرض قائمة الطلبات
    [ ] فتح التفاصيل
    [ ] إضافة تقييم الأداء
    [ ] الحفظ بنجاح

[ ] 4. لوحة إدارة التنقلات:
    [ ] عرض الإحصائيات
    [ ] الرسم البياني يعمل
    [ ] تشغيل الخوارزمية
    [ ] عرض النتائج
    [ ] الموافقة على التوزيعات

═══════════════════════════════════════════════
" -ForegroundColor Cyan

Write-Host "
🎯 النقاط المهمة للاختبار:
1. التنقل بين الأدوار يعمل بسلاسة
2. البيانات تحفظ بشكل صحيح
3. الحالات تتحدث بناءً على الإجراءات
4. الرسوم البيانية تعرض البيانات
5. الخوارزمية تعطي نتائج معقولة
" -ForegroundColor Green

